# Voice Denoiser - Система очистки голосовых сообщений от шума

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104.0-green)
![Telegram Bot](https://img.shields.io/badge/Telegram%20Bot-20.7-blue)
![License](https://img.shields.io/badge/License-MIT-yellow)

Полноценная система для очистки аудиозаписей от фонового шума с поддержкой нескольких алгоритмов обработки и Telegram-бота для удобного использования.

## 🎯 Функционал

### Основные возможности
- **5 методов очистки**: адаптивный, полосовая фильтрация, фильтр Винера, Noisereduce, ~~спектральное вычитание~~
- **Telegram-бот**: интерактивный интерфейс для обработки голосовых сообщений
- **REST API**: возможность интеграции с другими приложениями
- **Пакетная обработка**: одновременная очистка нескольких файлов
- **Поддержка форматов**: WAV, MP3, OGG, FLAC, M4A, AAC

### Особенности
- Настройка параметров под тип голоса
- Логирование и мониторинг
- Автоочистка временных файлов

## 🛠 Технологический стек

### Backend
- **Python 3.10+** - основной язык
- **FastAPI** - веб-фреймворк для REST API
- **Librosa, SoundFile** - обработка аудио
- **NumPy, SciPy** - научные вычисления
- **Noisereduce** - ML-библиотека для подавления шума

### Telegram Bot
- **python-telegram-bot** - асинхронный фреймворк
- **AIOHTTP** - асинхронные HTTP-запросы
- **python-dotenv** - управление конфигурацией

## 🚀 Быстрый старт

### 1. Клонирование репозитория
```bash
git clone https://github.com/maxsnapkov/voice-denoiser.git
cd voice-denoiser
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка Telegram бота
1. Откройте Telegram, найдите @BotFather
2. Создайте нового бота с помощью команды `/newbot`
3. Скопируйте полученный токен

### 4. Конфигурация
```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env файл
# Укажите токен вашего бота
BOT_TOKEN=ваш_токен_бота_здесь
```

### 5. Запуск системы

#### Два терминала
```bash
# Терминал 1: Запуск API сервера
python run_api.py

# Терминал 2: Запуск Telegram бота
python run_bot.py
```
## 📱 Использование Telegram бота

### Основные команды
- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/methods` - Показать доступные методы очистки
- `/settings` - Настройки очистки
- `/status` - Статус работы системы
- `/cancel` - Отменить текущую операцию

### Как очистить голосовое сообщение
1. Отправьте боту голосовое сообщение или аудиофайл
2. Выберите метод очистки + настройки (или используйте настройки по умолчанию)
3. Дождитесь обработки (обычно 5-30 секунд)
4. Получите очищенный файл

### Доступные методы очистки

| Метод | Описание | Рекомендации |
|-------|----------|--------------|
| **Adaptive** | Адаптивный метод (по умолчанию) | Автоматически выбирает лучший алгоритм |
| **Bandpass** | Полосовая фильтрация | Для удаления внеполосных шумов |
| **Spectral** | Спектральное вычитание | Классический метод для стационарных шумов |
| **Wiener** | Фильтр Винера | Адаптивный статистический метод |
| **Noisereduce** | ML-библиотека | Для сложных нестационарных шумов |

## 🌐 REST API

### Основные эндпоинты

```http
GET    /api/health           # Проверка здоровья API
GET    /api/methods          # Доступные методы очистки
POST   /api/denoise          # Очистка одного файла
POST   /api/denoise/batch    # Пакетная обработка
GET    /api/download/{file}  # Скачивание результата
GET    /api/stats            # Статистика API
```

### Пример использования API
```python
import requests

# Очистка аудиофайла
with open('noisy_audio.wav', 'rb') as f:
    response = requests.post(
        'http://localhost:8000/api/denoise',
        files={'audio_file': ('audio.wav', f, 'audio/wav')},
        data={'method': 'adaptive'}
    )
    
if response.status_code == 200:
    result = response.json()
    print(f"Обработано: {result['filename']}")
```

## 🔧 Консольное приложение

```bash
usage: python scripts/demo_cli.py [-h] [--method {bandpass,spectral_subtraction,wiener,noisereduce,adaptive}]
                                  [--voice-type {male,female,broadband}] [--sample-rate SAMPLE_RATE] [--plot] 
                                  [--compare] [--verbose] [--list-methods] [--version]
                                  input output

Очистка аудиофайлов от шума

positional arguments:
  input                 Путь к входному аудиофайлу
  output                Путь для сохранения очищенного файла

options:
  -h, --help            show this help message and exit
  --method {bandpass,spectral_subtraction,wiener,noisereduce,adaptive}, -m {bandpass,spectral_subtraction,wiener,noisereduce,adaptive}
                        Метод очистки (по умолчанию: noisereduce)
  --voice-type {male,female,broadband}, -vt {male,female,broadband}
                        Тип голоса для полосовой фильтрации
  --sample-rate SAMPLE_RATE, -sr SAMPLE_RATE
                        Целевая частота дискретизации (по умолчанию: 16000)
  --plot, -p            Показать графики до/после обработки
  --compare, -c         Сохранить сравнение (исходный + очищенный в один файл)
  --verbose, -v         Подробный вывод
  --list-methods        Показать список доступных методов и выйти
  --version             Показать версию и выйти

Примеры использования:
  python scripts/demo_cli.py input.wav output.wav
  python scripts/demo_cli.py input.wav output.wav --method bandpass
  python scripts/demo_cli.py input.wav output.wav --method noisereduce --verbose
  python scripts/demo_cli.py input.wav output.wav --voice-type female --plot
```

## 📁 Структура проекта

```
voice_denoiser/
├── app/                    # FastAPI приложение
│   ├── api/               # Эндпоинты API
│   ├── core/              # Ядро обработки аудио
│   ├── config.py          # Конфигурация
│   └── main.py            # Точка входа API
├── bot/                   # Telegram бот
│   ├── api_client.py      # Клиент для API
│   ├── handlers.py        # Обработчики сообщений
│   ├── keyboards.py       # Клавиатуры
│   ├── utils.py          # Вспомогательные функции
│   └── bot.py            # Основной файл бота
├── scripts/              # Вспомогательные скрипты
│   ├── demo_cli.py       # Консольный интерфейс
│   ├── batch_process.py  # Пакетная обработка
│   └── evaluate.py       # Оценка качества
├── notebooks/            # Исследовательские ноутбуки
│   ├── 01_audio_analysis.ipynb
│   └── 02_denoising_trials.ipynb
├── data/                 # Данные и модели
│   ├── raw/             # Исходные файлы
│   ├── processed/       # Обработанные файлы
│   └── models/          # Предобученные модели (возможность развития)
├── tests/               # Тесты
├── uploads/             # Загруженные файлы (создается автоматически)
├── processed/           # Обработанные файлы (создается автоматически)
├── temp/               # Временные файлы (создается автоматически)
├── .env.example        # Пример конфигурации
├── requirements.txt    # Зависимости
├── run_api.py         # Скрипт запуска API
├── run_bot.py         # Скрипт запуска бота
└── README.md          # Этот файл
```

## 📊 Оценка качества

### Метрики
Система оценивает качество очистки по следующим метрикам:
- **SNR (Signal-to-Noise Ratio)** - отношение сигнал/шум
- **PSNR (Peak Signal-to-Noise Ratio)** - пиковое отношение сигнал/шум
- **SI-SNR (Scale-Invariant SNR)** - масштабно-инвариантный SNR
- **Время обработки** - скорость работы алгоритма

### Сравнение методов
```
Метод              SNR улучшение    Время обработки
-----------------  ---------------  ----------------
Adaptive           12.5 дБ          0.8 сек
Bandpass           10.2 дБ          0.2 сек
Spectral           11.7 дБ          1.2 сек
Wiener             11.9 дБ          1.0 сек
Noisereduce        13.1 дБ          1.5 сек
```

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле [LICENSE](LICENSE).

## 🙏 Участники

- Снапков Максим
- Громов Рамиль

---

**Voice Denoiser** — магистерский проект, разработанный с ❤️ для улучшения качества голосовой коммуникации. Если проект оказался полезным, поставьте ⭐ на GitHub!